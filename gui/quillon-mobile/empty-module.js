/**
 * Empty module stub for Node.js packages that can't run in React Native.
 * Used by Metro to resolve modules like 'fs' that are required by
 * crystals-kyber/dilithium-crystals but will never actually execute
 * (pqCrypto.ts catches the import failure and uses simulated backend).
 */
module.exports = {};
