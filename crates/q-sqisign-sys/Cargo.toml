[package]
name = "q-sqisign-sys"
version.workspace = true
edition.workspace = true
description = "FFI bindings to the SQIsign post-quantum signature reference implementation"
license.workspace = true
authors.workspace = true
links = "sqisign"
build = "build.rs"

[build-dependencies]
cmake = "0.1"

[dependencies]
# No runtime dependencies — this is a pure -sys crate
