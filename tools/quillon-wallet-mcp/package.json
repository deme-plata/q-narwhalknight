{
  "name": "quillon-wallet-mcp",
  "version": "2.0.0",
  "description": "Quillon Wallet, Mining, DEX, Agent-Panel & QSHARE MCP server — every signed tool just works against production via the X-Wallet-Auth seed-derived signing path (file-first then env). Talk to Claude Code about wallets, mining, swaps, agent activity, QSHARE-1 treasury.",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@noble/curves": "^1.6.0",
    "@noble/hashes": "^1.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
