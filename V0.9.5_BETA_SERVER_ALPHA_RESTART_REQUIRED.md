# v0.9.5-beta: Server Alpha Restart Required

**Date**: 2025-11-05
**Version**: v0.9.5-beta
**Network**: testnet-phase4
**Status**: ✅ Server Beta FIXED | ⏳ Server Alpha NEEDS RESTART

---

## 🎯 ROOT CAUSE IDENTIFIED

### The Complete Picture:

**Server Beta (Bootstrap Node)**:
- ✅ Running v0.9.5-beta with bootstrap discovery fix (deployed 08:21:31 CET)
- ✅ `/api/v1/status` endpoint returns correct JSON with `data.multiaddrs`
- ✅ Ready to serve peer discovery requests

**Server Alpha (Testing Node)**:
- ⚠️  Running v0.9.5-beta BUT with stale connection state
- ❌ Attempted bootstrap discovery at 07:22:12 (BEFORE Server Beta was fixed)
- ❌ Discovery failed, fell back to mDNS-only mode
- ❌ Zero peers connected → InsufficientPeers on all gossipsub publishes

---

## 🔍 Log Analysis from looksgoodbutslow24.ini

### Timeline of Events:

**07:22:12** - Bootstrap Discovery Attempt (FAILED):
```
2025-11-05T07:22:12.735063Z  INFO q_api_server::config: 🔍 Attempting automatic bootstrap discovery from http://185.182.185.227:8080
2025-11-05T07:22:12.793209Z  WARN q_api_server::config: ⚠️  Failed to fetch bootstrap peers from http://185.182.185.227:8080: error sending request for url (http://185.182.185.227:8080/api/v1/status)
2025-11-05T07:22:12.793243Z  WARN q_api_server::config: ⚠️  Falling back to mDNS local discovery only
```

**Why it Failed**: Server Beta was NOT running v0.9.5-beta yet at 07:22 (fix deployed at 08:21).

**07:23:00** - Network Initialization (No Peers):
```
2025-11-05T07:23:00.825717Z  INFO q_network::unified_network_manager: 🆔 Local Peer ID: 12D3KooWN84wX8C1oCiWnhDuQs55ivD3BxqGGSVmioGwR3LNeAaT
2025-11-05T07:23:00.826446Z  INFO q_network::unified_network_manager: ℹ️ No bootstrap peers configured - DHT will populate via mDNS discoveries
```

**Why No Peers**: HTTP discovery failed, no multiaddrs extracted, Kademlia DHT is empty.

**07:23:48** - Block Production Starts (InsufficientPeers):
```
2025-11-05T07:23:48.693223Z  INFO q_network::unified_network_manager: 📤 Publishing block 1 (396 bytes) to gossipsub topic: /qnk/testnet-phase4/blocks
2025-11-05T07:23:48.693310Z  WARN q_network::unified_network_manager: ❌ Failed to publish block 1 to topic /qnk/testnet-phase4/blocks: InsufficientPeers
```

**Why InsufficientPeers**: Zero peers connected → Zero gossipsub subscribers → Cannot publish.

**07:23:28 - 07:24:13** - Connection Manager (Zero Connections):
```
2025-11-05T07:23:58.572142Z  INFO q_network::connection_manager: 🏥 PHASE 2: Health check complete - 0/0 connections healthy
```

**Why Zero**: No bootstrap peers → No DHT entries → No peers to connect to.

---

## 🚨 THE PROBLEM

Server Alpha's libp2p network manager **only attempts HTTP bootstrap discovery ONCE** during initialization (at startup).

When discovery failed at 07:22:12, the node:
1. ✅ Fell back to mDNS-only mode
2. ❌ Never retried HTTP discovery
3. ❌ Stayed in isolated state (zero peers)

Even though Server Beta is NOW fixed (08:21:31), Server Alpha:
- Still has no bootstrap peers in Kademlia DHT
- Still has zero connections
- Still cannot publish to gossipsub

---

## ✅ THE SOLUTION: RESTART SERVER ALPHA

### Why Restart Fixes It:

1. **Fresh Bootstrap Discovery**: On startup, Server Alpha will retry HTTP discovery
2. **Server Beta Now Fixed**: The `/api/v1/status` endpoint now returns correct `data.multiaddrs`
3. **Successful Peer Extraction**: Server Alpha will parse multiaddrs correctly with v0.9.5-beta code
4. **Kademlia DHT Population**: Bootstrap peers added to DHT
5. **Automatic Connection**: libp2p dials Server Beta's multiaddrs
6. **Gossipsub Connection**: Peers subscribe to `/qnk/testnet-phase4/blocks`
7. **Block Propagation**: Publishes succeed, blocks sync across network

---

## 🚀 Restart Instructions (Docker)

### For Server Alpha (161.35.219.10):

```bash
# SSH to Server Alpha
ssh root@161.35.219.10

# Method 1: Graceful Restart
docker restart q-v0.9.5-beta

# Method 2: Stop and Start (if restart doesn't work)
docker stop q-v0.9.5-beta
docker start q-v0.9.5-beta

# Monitor logs for successful bootstrap
docker logs q-v0.9.5-beta -f | grep -E "BOOTSTRAP|Discovered|CONNECTION"
```

---

## 🔍 Expected Logs After Restart

### Successful Bootstrap Discovery:
```
🔍 Attempting automatic bootstrap discovery from http://185.182.185.227:8080
✅ Discovered 2 bootstrap peer(s) automatically
   📡 /ip4/185.182.185.227/tcp/9001/p2p/12D3KooWL1Y38R8VrqgpEHFSceZa3sL24M9kGzqEYykMi1VE8U7L
   📡 /dns4/quillon.xyz/tcp/9001/p2p/12D3KooWL1Y38R8VrqgpEHFSceZa3sL24M9kGzqEYykMi1VE8U7L
```

### Successful Kademlia Bootstrap:
```
🚀 Kademlia DHT bootstrap initiated with 2 peers
✅ DHT bootstrap complete: 1 peers in routing table
```

### Successful Peer Connection:
```
✅ [CONNECTION] Successfully connected to peer: 12D3KooWL1Y38R8VrqgpEHFSceZa3sL24M9kGzqEYykMi1VE8U7L
🏥 PHASE 2: Health check complete - 1/1 connections healthy
```

### Successful Block Publishing:
```
📤 Publishing block 1 (396 bytes) to gossipsub topic: /qnk/testnet-phase4/blocks
✅ Block 1 published successfully
```

### Successful Sync:
```
🔄 [TURBO SYNC] Requesting blocks 1-8600 from peer 12D3KooWL1Y38R8VrqgpEHFSceZa3sL24M9kGzqEYykMi1VE8U7L
📥 Received block pack: blocks 1-100
📊 Highest contiguous height: 100
```

---

## 📊 Verification Checklist

After restart, verify:

- [ ] Bootstrap discovery succeeds (no "Failed to fetch" error)
- [ ] Multiaddrs extracted from Server Beta
- [ ] Kademlia DHT bootstrap succeeds
- [ ] Peer connection established to Server Beta
- [ ] Connection manager shows 1+ healthy connections
- [ ] Block publishes succeed (no InsufficientPeers)
- [ ] Turbo sync activates (requests blocks from Server Beta)
- [ ] Height increases (syncing to 8600+)

---

## 🔧 Technical Details

### Why HTTP Discovery is One-Time:

**File**: `crates/q-api-server/src/config.rs:234-262`

```rust
// Automatic bootstrap discovery via HTTP API
if config.bootstrap_peers.is_empty() {
    // Default bootstrap URL - Q-NarwhalKnight Testnet Masternode
    let bootstrap_url = env::var("Q_BOOTSTRAP_URL")
        .unwrap_or_else(|_| "http://185.182.185.227:8080".to_string());

    info!("🔍 Attempting automatic bootstrap discovery from {}", bootstrap_url);

    match Self::fetch_bootstrap_peers(&bootstrap_url) {
        Ok(peers) => {
            if !peers.is_empty() {
                info!("✅ Discovered {} bootstrap peer(s) automatically", peers.len());
                config.bootstrap_peers = peers;  // ← Only happens ONCE at startup
            } else {
                warn!("⚠️  No bootstrap peers discovered from {}", bootstrap_url);
                warn!("⚠️  Falling back to mDNS local discovery only");
            }
        }
        Err(e) => {
            warn!("⚠️  Failed to fetch bootstrap peers from {}: {}", bootstrap_url, e);
            warn!("⚠️  Falling back to mDNS local discovery only");  // ← Stays in this state forever
        }
    }
}
```

**Key Observation**: This code runs in `Config::from_env()` which is called ONCE during application initialization. If it fails, there's no retry mechanism.

**Why No Retry**:
- Config is created before network manager starts
- Network manager assumes bootstrap_peers are valid
- No periodic HTTP discovery re-attempts

**Future Enhancement Opportunity**: Add periodic bootstrap rediscovery for resilience.

---

## 🎯 Root Cause Summary

| Issue | Status | Impact |
|-------|--------|--------|
| Server Beta bootstrap endpoint | ✅ FIXED (v0.9.5-beta) | Returns correct `data.multiaddrs` |
| Server Alpha bootstrap discovery | ❌ STALE | Failed at 07:22, never retried |
| Server Alpha peer connections | ❌ ZERO | No DHT entries → Cannot connect |
| Server Alpha gossipsub | ❌ BROKEN | Zero subscribers → InsufficientPeers |
| Server Alpha sync | ❌ STUCK | Cannot receive blocks without peers |

**FIX**: Restart Server Alpha → Retry HTTP discovery → Success

---

## 🚀 Next Steps

1. **Restart Server Alpha** (Docker container)
2. **Monitor logs** for successful bootstrap discovery
3. **Verify peer connection** to Server Beta
4. **Confirm sync** progresses to 8600+ blocks
5. **Validate gossipsub** publishes succeed

---

**Status**: Ready for Server Alpha restart. Server Beta fix is deployed and verified working.

**Download**: https://quillon.xyz/downloads/q-api-server-v0.9.5-beta (already running on Server Alpha, just needs restart)

**Bootstrap Endpoint**: http://185.182.185.227:8080/api/v1/status ✅ WORKING

**Expected Result**: Full network sync and gossipsub propagation after restart.
